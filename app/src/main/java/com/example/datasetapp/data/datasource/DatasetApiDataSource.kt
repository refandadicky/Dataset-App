package com.example.datasetapp.data.datasource

import com.example.datasetapp.data.source.network.service.DatasetApiService

/*
class DatasetApiDataSource(
    private val service: DatasetApiService
) : DatasetDataSource {
    override suspend fun uploadImage(urlPayload: UrlPayload): UploadMultipartResponse {
        return service.uploadImage(urlPayload)
    }
}

interface DatasetDataSource {
    suspend fun uploadImage(urlPayload: UrlPayload): UploadMultipartResponse
}*/
